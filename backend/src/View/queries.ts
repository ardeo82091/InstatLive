class Queries {
    username: string;
    statement: string;

    constructor(username: string, statement: string) {
        this.username = username;
        this.statement = statement;
    }
}

export default Queries;